<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kushagra.smartmeds">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AlarmSystem.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MoreAbout.MedicineDetails.MedicineDetailsActivity" />
        <activity android:name=".MoreAbout.ImportantContact.ImportantContactActivity" />
        <activity android:name=".LoginSystem.LoginActivity" />
        <activity android:name=".LoginSystem.RegisterActivity" />
        <activity android:name=".MoreAbout.MedicineDetails.AddNewMedActivity" />
        <activity android:name=".MoreAbout.MedicineDetails.Class.MedicineSingleActivity" />
        <activity android:name=".AlarmSystem.AddAlarmActivity"></activity>
    </application>

</manifest>